id: ssrf_import_preview
kind: true_positive
vuln_class: ssrf
# Ground truth: /api/imports/preview accepts any loopback http URL and fetches it.
vulnerable: true
expect: VERIFIED

focus: |
  Restrict this run to the import preview endpoint in routes/fetch.py. Establish
  which URLs that endpoint will and will not fetch on the caller's behalf, and
  test your conclusion against the running target.

scope_paths:
  - '^/api/imports/preview$'

max_turns: 14
budget_usd: 1.0
